#define BLYNK_PRINT Serial

/*
 * BLYNK_AUTH_TOKEN
 * Blynk authentication token (required)
 */
#define BLYNK_AUTH_TOKEN "PASTE YOUR BLYNK AUTHENTICATION TOKEN"

/*
 * BLYNK_CUSTOM_HOST_NAME
 * Custom Blynk server hostname (required)
 */
#define BLYNK_CUSTOM_HOST_NAME "your.custom.hostname"

/*
 * BLYNK_CUSTOM_PORT
 * Custom Blynk server port number (required)
 */
#define BLYNK_CUSTOM_PORT 8080

/*
 * MANUAL_SSID
 * SSID for manual WiFi connection (optional, use with initWiFiManually())
 */
#define MANUAL_SSID "SSID Name"

/*
 * MANUAL_PASSWORD
 * Password for manual WiFi connection (optional, use with initWiFiManually())
 */
#define MANUAL_PASSWORD "wifi password"

/*
 * ACCESS_POINT_NAME
 * Name of the access point to be created if WiFi connection fails (required)
 */
#define ACCESS_POINT_NAME "AP Name"

/*
 * ACCESS_POINT_PASSWORD
 * Password for the access point to be created if WiFi connection fails (optional)
 */
#define ACCESS_POINT_PASSWORD "AP Password"